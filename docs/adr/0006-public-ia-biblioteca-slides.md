# Public IA starts from Biblioteca | Slides

Public IA starts from a sticky Alvo-style header with mode pills **Biblioteca | Slides** (prototype variant C). Song browse mirrors Alvo (search, Músicas/Artistas). The builder product area is labeled **Slides** (routes under `/slides…`); the domain entity remains Program with link-only cookie ownership (ADR 0001). A left mode rail and CifraClub-first browse were rejected as the starting shell. Primary source: branch `prototype/public-ia`.
